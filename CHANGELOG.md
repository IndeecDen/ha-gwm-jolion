# Changelog

## 0.1.0-beta.1

Первая бета-версия.

- Компактные ползунки мощности 1–3 и таймера сидений, отдельные переключатели выключения и фактический статус.
- Кнопка подогрева обоих сидений на уровне 3 на 5 минут в редакторе и стандартном наборе кнопок пульта.

- Обновлено превью основной карточки и руководство пользователя.
- Проверены вызовы команд, фактические статусы, ползунки и адаптивность пульта. Проверка новых команд на автомобиле остаётся отдельным этапом.

После обновления перезапустите Home Assistant и обновите страницу панели. В существующей конфигурации пульта новую кнопку можно включить через редактор → «Кнопки управления» → «Подогрев обоих сидений».

## 0.1.0-alpha.23

- Убраны команды обогревов руля и заднего стекла из карточек, кнопок и настроек оборудования: управление не подтверждено, в диагностике получен тайм-аут GWM 2004. Старые действия отклоняются сразу.
- Добавлена заметная индикация фактического подогрева каждого сиденья, независимо от выбранных настроек.

После обновления перезапустите Home Assistant и обновите страницу панели. Старые кнопки неподтверждённых обогревов удаляются при загрузке интеграции.

## 0.1.0-alpha.22

- Управление подогревами передних сидений (0–3) и таймером (1–10 минут) в разделе климата основной карточки и отдельном действии HA.
- Формат T5 окон приведён к Android GWM 2.3.1; возвращена кнопка окон в основной карточке.
- Выбор записи интеграции для команд: при нескольких автомобилях требуется entry_id.
- Обновлены README, roadmap и превью основной карточки.
- Команды сидений и исправленные команды окон пока не подтверждены испытанием на автомобиле.

После обновления перезапустите Home Assistant и обновите страницу панели.

## 0.1.0-alpha.21

### Карточка-пульт в оформлении StarLine

- `gwm-jolion-remote-card` получила графику, расположение автомобиля, круглые кнопки и анимации выхлопа и ожидания команды из [Anonym-tsk/lovelace-starline-card](https://github.com/Anonym-tsk/lovelace-starline-card).
- Прежний вид сверху заменён универсальной иллюстрацией автомобиля спереди. Двери показываются общим индикатором, багажник — отдельным слоем; индивидуальная анимация четырёх стёкол убрана, текстовые статусы и сущности окон сохранены.
- Подсветка вокруг автомобиля связана с закрытым центральным замком; при неизвестном состоянии замка подсветка не включается. Выхлоп отображается при работающем двигателе, дополнительный вентилятор — при включённом климате.
- Добавлены светлая и тёмная темы с автоматическим выбором по теме Home Assistant. В YAML доступен параметр `dark`, в редакторе — переключатель тёмного оформления. Старый `car_color` принимается для совместимости, но больше не меняет иллюстрацию.
- Сохранены команды GWM, подтверждения действий и ограничения alpha.20. Порядок кнопок по умолчанию: двигатель, замок, климат, багажник, обновление; пользовательский `controls` сохраняет свой порядок.
- Обновления телеметрии сохраняют DOM-элементы, фокус кнопок и ход анимаций. Добавлена поддержка уменьшения движения и адаптивная сетка статусов.
- Изображения обеих тем встроены в файл карточки; внешние запросы за графикой не нужны. Добавлены `LICENSE.starline` и `THIRD_PARTY_NOTICES.md`: адаптированная remote-карточка распространяется под GPL-3.0-only, остальные файлы сохраняют свои лицензии.
- Версия интеграции, карточки-пульта и ключи кэша фронтенда обновлены до `0.1.0-alpha.21`; протокол и payload команд не менялись.
- Добавлены браузерные проверки вызовов команд на тестовых данных, обновления подписей и состояний, сохранения DOM, обеих тем, ширины 280/400/700 px и уменьшения движения. Проверены встроенные PNG и соответствие версии ресурса; добавлены превью и обновлён README. Проверка на реальном автомобиле для этого изменения не проводилась.

После установки обновлённых файлов необходимо перезапустить Home Assistant и обновить страницу панели.

## 0.1.0-alpha.20

Крупный cleanup-релиз пользовательского слоя Home Assistant. После полевых тестов интеграция перестаёт превращать каждый исследовательский raw-код в отдельную сущность и оставляет в Entity Registry только полезные рабочие кейсы.

### Entity Registry cleanup

- Config Entry schema повышена до v2 и добавлена одноразовая миграция существующих установок;
- миграция удаляет старые TPMS raw/status, window raw/window-learn, light raw, indicator raw, служебные vehicle/model/service entities и diagnostic helper entities;
- удаляются дублирующие кнопки `lock_vehicle` / `unlock_vehicle`: каноническим управлением становится `lock` entity;
- удаляется дублирующий `vehicle_unlocked` binary sensor;
- удаляется `gps_authorized`: для карты остаётся стандартный `device_tracker`, а факт наличия GPS доступен в attributes T-Box;
- индивидуальные окна пересоздаются как обычные user-facing binary sensors, а их `raw_state` переносится в attributes;
- live-уровни подогрева водительского и пассажирского сидений остаются полезными sensor entities с чистыми friendly names;
- raw protocol data не удаляются из parser/coordinator и продолжают попадать в Protocol Capture/diagnostics.

### Оптимизированный рабочий набор

Обычные sensors: запас хода, топливо L/%, пробег, давление/температура четырёх шин, уровни двух передних подогревов сидений и timestamp последнего обновления.

Обычные binary sensors: T-Box online, двигатель, общий статус дверей + 4 двери, общий статус окон + 4 окна, багажник и климат.

Native entities остаются основным способом управления:

- `lock` — открыть/закрыть автомобиль;
- `climate` — ON/OFF и температура;
- `number` — время работы климата;
- `device_tracker` — местоположение.

При Remote Control + PIN создаются только кнопки с полезным рабочим сценарием: engine start/stop, flash/horn, trunk open/close, rear defrost ON/OFF и steering heat ON/OFF. `Обновить данные` создаётся всегда.

### Убрано из обычного UI

- `close_windows` больше не создаёт кнопку: cloud response был успешным, но физического закрытия стекол в тесте не произошло;
- `open_windows`, panorama/sunshade, front-defrost и Cabin Clean не создают entity buttons;
- экспериментальные/неподтверждённые comfort status binary sensors удалены;
- экспериментальные опции оборудования убраны из обычного Options Flow; остаются только обогрев руля, заднего стекла и status двух передних подогревов сидений.

Низкоуровневые `gwm_jolion.*` services сохранены для обратной совместимости и протокольных тестов, но bundled cards больше не предлагают неподтверждённые действия.

### Bundled cards

- добавлен alpha.20 compatibility layer, который позволяет карточкам работать без удалённых raw/helper entities;
- raw уровни четырёх окон читаются из `raw_state` соответствующих window binary sensors;
- lock-status читается из native `lock` entity;
- GSM/model/oil/GPS/feature flags читаются из attributes `T-Box онлайн`;
- last-command и Protocol Capture metadata читаются из attributes кнопки `Обновить данные`;
- в основной карточке исправлена историческая pre-field-test перестановка FL/FR и RL/RR окон;
- физически неподтверждённое управление окнами скрыто, при этом статус и анимация четырёх окон сохранены;
- remote-card/editor фильтруют experimental controls и неподтверждённый `Defrost` status;
- фронтенд cache key обновлён до `0.1.0-alpha.20`.

### Protocol / telemetry

Новые GWM payload в этом релизе не угадываются. Сохраняются результаты recovered marker-теста:

- `2210001=FL`, `2210002=FR`, `2210003=RL`, `2210004=RR`;
- `2210001`: `1=closed`, `3=partial`, `2=full open`; generic window decoder: `1=closed`, `2/3=not closed`;
- `2016001`: `0=OFF`, `2=RUNNING`; отдельный ACC signal не найден;
- `2202001`: `0=climate OFF`, `1=climate ON`;
- `2204008` остаётся только кандидатом дальнего света;
- отдельные live status для steering/rear/front-defrost не считаются подтверждёнными.

### Migration note

`alpha.20` намеренно является breaking cleanup для alpha-ветки. Автоматизации, напрямую использовавшие удалённые raw/helper entity IDs, нужно перевести на нормальные user-facing entities либо использовать Protocol Capture/diagnostics. Старые JSONL capture-файлы в `/config/gwm_jolion_protocol_capture/` не удаляются.

## 0.1.0-alpha.19.7

Полевой protocol-релиз по восстановленной JSONL-сессии `alpha.19.5` с 68 записями и marker-тестами.

### Confirmed telemetry

- физически закреплены все четыре окна: `2210001=FL`, `2210002=FR`, `2210003=RL`, `2210004=RR`;
- для FL подтверждена шкала `1=closed`, `3=partial`, `2=full open`; для FR/RL/RR надёжно пойманы `1=closed` и `2=full open`, поэтому generic decoder сохраняет консервативное `2/3=not closed`;
- `2202001` переведён в confirmed climate state: `0=OFF`, `1=ON`;
- marker ACC подтвердил, что `2016001` остаётся `0` в ACC, а RUNNING даёт `0→2`, STOPPED — `2→0`; отдельный ACC signal не найден;
- `2204008` повышен из unknown в `known_unverified` как кандидат дальнего света: после `LIGHT_HIGH` пойман `0→1`, затем возврат `1→0`;
- быстрые steering/rear/front-defrost маркеры не выявили отдельных live status-кодов в 43-item STATUS; front defrost активирует общий climate code и не используется как отдельный status.

### Home Assistant / remote card

- raw telemetry `2210001..2210004` теперь создаёт diagnostic sensors с физическими friendly names;
- существующие window binary-sensor unique IDs сохранены, их названия заменены на физические позиции;
- remote-card обнаруживает raw/binary entities всех четырёх окон и анимирует соответствующие стёкла отдельно;
- фары по-прежнему не активируются по engine и не включаются по кандидату `2204008` до повторного light-test;
- frontend cache key и версия обновлены до `0.1.0-alpha.19.7`.

### Capture recovery

- README уточняет, что новый setup/reload создаёт новый timestamped JSONL, но старые capture-файлы в `/config/gwm_jolion_protocol_capture/` не удаляются; стандартная диагностика показывает текущую активную сессию.

## 0.1.0-alpha.19.6

UX-релиз `custom:gwm-jolion-remote-card` по результатам реального скриншота `alpha.19.5`. Протокол автомобиля и remote payload не изменялись.

### Wide SUV top-view

- центральный SVG автомобиля переработан: кузов стал заметно шире и выше и больше не выглядит как длинная узкая «лодка»;
- viewBox увеличен до `1100×520`;
- добавлены более выраженные детали капота, решётки, крыши, салона, панорамной секции, рейлингов и зеркал;
- ориентация не менялась: капот слева, багажник справа, FR/RR сверху, FL/RL снизу;
- независимые анимации дверей, багажника, двигателя и климата сохранены;
- водительское окно `2210001` остаётся на FL с подтверждённой шкалой `1=закрыто`, `3=частично`, `2=полностью открыто`;
- слой фар остаётся визуально подготовленным, но не активируется до подтверждения telemetry-кода света;
- капот остаётся статическим до обнаружения достоверного hood-status.

### Status values

- восемь стандартных статусных плиток теперь всегда используют сетку `4×2`, а не пытаются уместиться в 8 узких колонок;
- значения топлива, пробега и остальных состояний получили отдельную текстовую область и больше не скрываются за ellipsis;
- `Топливо` и `Пробег` выводят фактическое состояние Home Assistant вместе с единицей измерения;
- недоступные значения явно отображаются как `Нет данных`;
- на очень маленькой ширине контейнера статусы автоматически переходят в 2 колонки;
- в visual editor добавлен опциональный статус `Defrost`;
- кнопки управления остаются отдельным нижним рядом.

### Tests / docs / version

- обновлены regression-тесты геометрии и видимости значений статусных плиток;
- `manifest.json`, internal `VERSION` и frontend cache key обновлены до `0.1.0-alpha.19.6`;
- обновлены `README.md`, `ROADMAP.md` и `CHANGELOG.md`;
- новых GWM remote payload, telemetry mappings и protocol assumptions нет.

## 0.1.0-alpha.19.5

UX-релиз `custom:gwm-jolion-remote-card` после проверки `alpha.19.4` на реальном узком Lovelace-экране. Протокол автомобиля и remote payload не изменялись.

### Исправленная ориентация

- капот остаётся слева, багажник справа;
- **верх сцены теперь соответствует правой стороне автомобиля**, низ — левой;
- FR/RR размещены сверху, FL/RL снизу;
- ось вращения каждой двери по-прежнему жёстко закреплена на левой кромке своей SVG-створки;
- FR/RR открываются наружу вверх, FL/RL — наружу вниз;
- regression-тест обновлён так, чтобы CI блокировал повторное перепутывание сторон.

### Новый layout remote-card

- автомобиль заметно увеличен и занимает почти всю центральную ширину сцены;
- уменьшено количество пустого пространства вокруг автомобиля;
- дверные подписи вынесены в HTML-callouts поверх сцены и стали читаемее на узкой карточке;
- багажник остаётся отдельным анимируемым слоем справа;
- engine overlay продолжает показывать оранжевую пульсацию моторного отсека;
- climate overlay продолжает показывать вентилятор и синие воздушные потоки;
- Online/Offline и GSM сохранены справа сверху;
- восемь статусных плиток визуально отделены от ряда управления;
- на узком экране статусы перестраиваются в 4×2, кнопки управления — в 3 колонки;
- водительское окно `2210001` остаётся на передней левой двери и использует подтверждённую шкалу `1=закрыто`, `3=частично`, `2=полностью открыто`;
- остальные окна не получают неподтверждённую физическую привязку;
- фары по-прежнему не связываются с двигателем и не активируются до light marker-теста;
- капот остаётся статичным до подтверждённого hood telemetry.

### Tests / docs / version

- frontend version и cache key обновлены до `0.1.0-alpha.19.5`;
- обновлены `README.md`, `ROADMAP.md`, `CHANGELOG.md`, `manifest.json` и internal `VERSION`;
- обновлён `tests/test_remote_card_geometry.py` под ориентацию FR/RR сверху и FL/RL снизу;
- новых GWM remote payload, новых telemetry mappings и новых protocol assumptions нет.

## 0.1.0-alpha.19.4

Полная визуальная переработка `custom:gwm-jolion-remote-card` по реальной ориентации автомобиля: горизонтальный вид сверху, капот слева, багажник справа, левая сторона автомобиля сверху.

### Animated remote card

- центральная SVG-сцена полностью перерисована в тёмном стиле GWM/Jolion;
- добавлена отдельная геометрия четырёх дверей: передняя левая, передняя правая, задняя левая и задняя правая;
- для всех четырёх дверей ось вращения жёстко закреплена на **левой кромке своей двери** по направлению автомобиля;
- верхние FL/RL двери открываются наружу вверх, нижние FR/RR — наружу вниз;
- для каждой двери отображается собственное состояние и независимая анимация по существующим binary sensors;
- добавлены отдельные hinge-маркеры и regression-тест, чтобы left/right и оси дверей больше не перепутывались;
- багажник получил отдельный SVG-слой с плавным отводом крышки и красной подсветкой при открытии;
- работающий двигатель показывает оранжевую пульсацию моторного отсека и отдельный engine glyph;
- включённый климат показывает вращающийся вентилятор и анимированные воздушные потоки в салоне;
- водительское переднее окно продолжает использовать подтверждённую шкалу `2210001`: `1=закрыто`, `3=частично открыто`, `2=полностью открыто`;
- остальные окна до завершения marker-теста не получают выдуманную физическую привязку и при общем `windows_open` подсвечиваются как общий alert;
- фары оставлены отдельным SVG/CSS-слоем и не связаны с двигателем; активация будет добавлена только после физического подтверждения light telemetry;
- капот остаётся статическим до появления подтверждённого hood-status.

### Status UI

- в сцене слева добавлен GWM / JOLION branding;
- справа сверху отображаются T-Box `Онлайн / Оффлайн` и 4-сегментный GSM-индикатор;
- нижняя строка теперь по умолчанию содержит восемь компактных карточек: двигатель, климат, топливо, пробег, двери, окна, багажник и замок;
- двери показывают число реально открытых створок, если доступны четыре индивидуальных door sensors;
- locked/unlocked подсвечиваются разными состояниями без попытки выдавать центральный замок за отдельный подтверждённый alarm-status;
- адаптивная раскладка переводит восемь статусов в сетку 4×2 на узком экране.

### Tests / docs / version

- добавлен `tests/test_remote_card_geometry.py` с проверкой ориентации FL/FR/RL/RR, осей петель и независимости фар от двигателя;
- frontend проходит `node --check`;
- обновлены `README.md`, `ROADMAP.md`, `CHANGELOG.md`, `manifest.json`, internal `VERSION` и frontend cache key;
- новых GWM remote payload и новых предположений о telemetry-кодах в этой версии нет;
- версия интеграции — `0.1.0-alpha.19.4`.

## 0.1.0-alpha.19.3

Полевой и frontend-релиз: исправлена трактовка живых состояний сидений/окон и полностью переработана карточка `custom:gwm-jolion-remote-card`.

### Remote card: вид сверху

- SVG автомобиля переведён на вид сверху: капот слева, багажник справа;
- кузов подсвечивается по состоянию замка: locked / unlocked / unknown;
- в правом верхнем углу отображаются T-Box Online/Offline и 4-сегментный GSM-индикатор;
- под автомобилем добавлена компактная строка статусов: двигатель, климат, топливо, пробег, двери, окна, багажник;
- отдельные двери анимируются по существующим door binary sensors;
- открытый багажник визуально отводится от кузова;
- работающий двигатель подсвечивает моторный отсек пульсацией;
- включённый климат показывает анимированные потоки воздуха в салоне;
- водительское окно `2210001` визуально использует подтверждённую шкалу `1=закрыто`, `3=частично открыто`, `2=полностью открыто`;
- остальные окна пока не получают выдуманную физическую позицию: при общем `windows_open` показывается общий visual alert;
- фары вынесены в отдельный SVG/CSS-слой и больше не зависят от состояния двигателя; слой останется выключенным до подтверждения telemetry-кода света;
- капот остаётся статическим до появления подтверждённого hood status.

### Окна

- старый decoder `raw == 0 -> open` удалён;
- `raw=1` теперь трактуется как закрыто;
- `raw=2` и `raw=3` трактуются как незакрытое окно;
- неизвестные значения, включая `0`, не угадываются и возвращают unknown;
- aggregate `windows_open` учитывает multistate-значения;
- индивидуальные window binary sensors временно имеют нейтральные названия по коду, пока marker-тест не подтвердит физическую позицию `2210002..2210004`.

### Подогрев передних сидений

- `vehicleBasicsInfo.leftFrontSeat/rightFrontSeat` больше не используются как fallback живого состояния;
- живое состояние берётся только из `2220001` / `2220002`;
- принята шкала для обоих передних сидений: `0=off`, `1=low`, `2=medium`, `3=high`;
- если live-сигнал отсутствует, сущность остаётся unavailable вместо ложного уровня из сохранённого preset.

### Tests / docs / version

- добавлены тесты multistate-window decoder;
- добавлен тест, что `vehicleBasicsInfo` не подменяет live seat state;
- добавлен тест приоритета `2220001/2220002`;
- обновлены `README.md`, `ROADMAP.md`, `PROTOCOL.md`, `manifest.json`, internal `VERSION` и frontend cache version;
- версия интеграции — `0.1.0-alpha.19.3`.

## 0.1.0-alpha.19.2

Расширенный диагностический релиз перед физическими тестами автомобиля. Цель — не потерять потенциально полезные поля GWM, которые раньше не попадали в Protocol Capture.

### Protocol Capture schema 2

- schema JSONL повышена до `2`;
- для каждого `items[].code` сохраняется `signal_units`, если GWM прислал единицу измерения;
- сохраняется `signal_item_seen_keys` — набор имён полей, реально встреченных внутри элементов `items[]`;
- добавлен `status_meta` с безопасными верхнеуровневыми полями `getLastStatus`: `acquisitionTime`, `uploadTime`, `updateTime`, `oilQty`, `percentageOfOil`, `charge`, `serviceStatus`, `deviceType`, `command`;
- для `status_meta` сохраняется diff `previous → value` в `status_meta_changes`;
- добавлен `status_structure` — структура всего `getLastStatus` только с именами ключей и типами, без значений; благодаря этому неизвестные верхнеуровневые и вложенные поля можно заметить без риска утечки VIN/координат;
- добавлены `vehicle_basics_seen_keys` и `vehicle_basics_structure`, чтобы видеть неизвестные поля `vehicleBasicsInfo`, даже если их значения не входят в безопасный whitelist;
- добавлены `tbox_meta`, `tbox_meta_changes` и `tbox_structure` для безопасного анализа `findStatus` / T-Box;
- потенциально чувствительные неизвестные значения автоматически не сохраняются: для них остаётся только структура ключей/типов;
- VIN, точные координаты, телефон/аккаунт, credentials, IMSI/ICCID, vehicle/device ID по-прежнему не записываются в Protocol Capture.

### GPS / карта Home Assistant

- существующая сущность `device_tracker` **Местоположение** остаётся источником GPS для стандартной карты Home Assistant;
- latitude/longitude теперь безопасно приводятся к `float`, чтобы строковые координаты GWM корректно отображались на карте;
- для GPS-трекера добавлена иконка `mdi:car`;
- координаты используются только самой GPS-сущностью и не добавляются в Protocol Capture/диагностический экспорт.

### Тесты / документация

- добавлены unit-тесты расширенного capture record: units, `percentageOfOil`, safe metadata, structures и изменения meta-полей;
- обновлены `README.md`, `ROADMAP.md`, `CHANGELOG.md`, `manifest.json` и внутренний `VERSION`;
- новых GWM remote-команд, command payload и предположений о назначении неизвестных кодов нет;
- версия интеграции — `0.1.0-alpha.19.2`.

## 0.1.0-alpha.19.1

Диагностический hotfix Protocol Capture: длинная тестовая сессия больше не может вытеснить исходный telemetry baseline из стандартного экспорта Home Assistant.

### Экспорт Protocol Capture

- лимит свежих записей в `protocol_capture → session_export` увеличен с 2000 до **5000**;
- первый корректный `type=refresh` с `baseline=true` сохраняется отдельно и **не входит** в лимит 5000;
- при длинной сессии экспорт содержит **первый baseline + 5000 самых свежих корректных записей**, максимум 5001 запись;
- короткая сессия экспортируется целиком;
- полный JSONL-журнал на диске не обрезается и продолжает хранить всю сессию в `/config/gwm_jolion_protocol_capture/`;
- если baseline отсутствует в повреждённом/старом журнале, экспорт сохраняет до 5000 последних корректных записей и выставляет `baseline_preserved=false`;
- добавлены поля `valid_records_in_file`, `baseline_preserved`, `recent_records_limit` и `selection=first_baseline_plus_latest`;
- `truncated` теперь показывает, что часть корректных записей не вошла именно в ограниченную копию диагностики;
- добавлены unit-тесты сохранения baseline, выбора последних записей, полного экспорта короткой сессии и значения лимита 5000.

### Документация / версия

- обновлены `README.md`, `ROADMAP.md`, `CHANGELOG.md`, `manifest.json` и внутренний `VERSION`;
- новых GWM remote-команд, payload или telemetry mappings нет;
- версия интеграции — `0.1.0-alpha.19.1`.

## 0.1.0-alpha.19

Подготовка Protocol Capture к полевой диагностической сессии без добавления новых GWM remote-команд.

### Диагностика Protocol Capture

- стандартная команда Home Assistant `Скачать диагностику` теперь включает текущую JSONL capture-сессию прямо в диагностический JSON;
- экспорт ограничен 2000 корректными записями, чтобы очень длинная сессия не раздувала diagnostics без ограничений;
- экспорт сообщает `records_in_file`, `records_exported`, `truncated`, `invalid_lines` и `read_error`;
- в diagnostics дополнительно сохраняются время последней capture-записи, источник последнего обновления, число последних изменений и последняя пользовательская метка;
- автоматические capture-записи по-прежнему не содержат VIN, точные координаты, телефон/аккаунт, credentials и device identifiers;
- пользовательские метки явно помечены как user-supplied и не должны использоваться для персональных или секретных данных.

### Сенсор и метки тестов

- добавлен диагностический сенсор `Запись протокола GWM` со состояниями `disabled`, `recording`, `error`;
- атрибуты сенсора показывают имя файла, число записанных строк, время и источник последней записи, `last_changes_count`, последнюю метку и ошибку записи;
- добавлен сервис `gwm_jolion.add_capture_marker` с полем `label`;
- метка записывается отдельной JSONL-строкой `type=marker` и не изменяет telemetry baseline / предыдущие raw-значения;
- метки нормализуются в одну строку и ограничены 80 символами;
- baseline теперь определяется наличием предыдущего telemetry snapshot, поэтому даже метка до первого refresh не может сломать первый baseline.

### Тесты / протокол

- добавлены unit-тесты marker records, проверки пустых/слишком длинных меток и bounded diagnostics export;
- `ROADMAP.md` обновлён для `alpha.18` и `alpha.19`;
- новых GWM command payload, remote-команд и предположений о неизвестных кодах в этой версии нет;
- версия интеграции обновлена до `0.1.0-alpha.19`.

## 0.1.0-alpha.18

Добавлен отдельный режим записи протокола для физических тестов автомобиля и поиска неизвестных GWM-кодов.

### Режим поиска GWM-кодов

- в настройках интеграции добавлен переключатель `Режим поиска GWM-кодов (JSONL-журнал)`, по умолчанию выключенный;
- при включении создаётся отдельная сессия в `/config/gwm_jolion_protocol_capture/gwm_code_search_*.jsonl`;
- каждый успешный опрос GWM дописывается в тот же файл одной независимой JSONL-строкой;
- первый успешный опрос записывается как baseline с полным снимком всех увиденных raw-сигналов и безопасного `vehicleBasicsInfo`;
- последующие опросы содержат полный снимок и вычисленный diff `previous -> value` для GWM-сигналов и `vehicleBasicsInfo`;
- неизвестные telemetry-коды сохраняются вместе с текущими значениями, чтобы можно было сопоставлять их с физическими действиями в автомобиле;
- для известных кодов в diff добавляются имя сигнала, описание и текущий статус проверки протокола;
- ручная кнопка `Обновить данные` помечает запись как `source=manual_button`;
- обычный coordinator polling помечается как `source=poll`;
- опрос после успешной удалённой команды помечается как `source=remote_command`;
- проверочный опрос перед удалённым запуском двигателя помечается как `source=remote_start_guard`;
- запись JSONL выполняется вне event loop, а ошибка записи журнала не прерывает штатный polling автомобиля.

### Диагностика и приватность

- capture-журнал не включает VIN, точные координаты, телефон/аккаунт, пароли, PIN, access token, IMSI/ICCID, vehicle ID и device identifiers;
- в Home Assistant diagnostics добавлены состояние Protocol Capture, имя активного файла, число успешно записанных строк и последняя ошибка записи;
- добавлены unit-тесты baseline, diff известных/неизвестных сигналов и последовательного JSONL append;
- Python checks, Hassfest и HACS validation проходят для нового режима.

### Протокол / remote-команды

- новых remote-команд или новых GWM command payload в этой версии не добавлялось;
- существующие remote-команды продолжают работать без изменения их payload;
- версия интеграции обновлена до `0.1.0-alpha.18`.

## 0.1.0-alpha.17

Исправлена регистрация визуального редактора основной карточки `custom:gwm-jolion-card`.

- editor-модуль теперь ожидает регистрацию основной карточки через `customElements.whenDefined()`;
- bundled editor загружается перед основной карточкой и больше не зависит от случайного порядка исполнения frontend-ресурсов Home Assistant;
- frontend URL переведены на cache key `0.1.0-alpha.17`;
- временные служебные workflow, использованные при поиске причины, удалены, обычный CI восстановлен;
- remote-команды и GWM payload не изменялись.

## 0.1.0-alpha.16

Добавлен визуальный редактор основной Lovelace-карточки `custom:gwm-jolion-card`.

### Основная карточка

- можно выбирать отображаемые кнопки: двигатель, замок, багажник, окна, климат, обновление и доступные comfort-функции;
- можно выбирать информационные элементы: топливо, запас хода, пробег, предупреждения, шины, T-Box, GSM, GPS, масло, последнюю команду, время обновления и статусы подогревов;
- отдельно настраиваются таймер двигателя, температура и таймер климата;
- существующие карточки без новых параметров сохраняют прежний полный набор элементов;
- настройки комплектации интеграции имеют приоритет над настройками карточки;
- frontend editor поставляется отдельным модулем, код remote-команд не менялся;
- новых GWM remote payload нет.

## 0.1.0-alpha.14

Исправление зависания очереди удалённых команд и отображения неизвестных состояний в карточках.

### Удалённые команды

- конечные ошибки T5 теперь завершают polling сразу вместо ожидания полного 300-секундного timeout;
- коды `1000`, `2000` и подтверждённый промежуточный код `11` продолжают считаться незавершёнными;
- после выхода из command lock публикуется отдельное обновление `in_progress=false`, поэтому карточки больше не остаются заблокированными после завершённой/ошибочной команды;
- карточки ориентируются на фактический `in_progress`, а не на потенциально устаревшее состояние `pending`;
- новых remote payload автомобиля нет.

### Карточки / статусы

- основная карточка не предлагает действие по замку, двигателю, климату, окнам или багажнику, если соответствующая телеметрия недоступна;
- карточка-пульт показывает `нет данных` вместо ложных `закрыто`, `выключено`, `offline` для неизвестных основных состояний;
- информационные плитки GSM/T-Box, GPS и климата также различают реальное отрицательное состояние и отсутствие данных;
- добавлены unit-тесты классификации T5 result codes.

## 0.1.0-alpha.13

Добавлена вторая встроенная Lovelace-карточка `custom:gwm-jolion-remote-card` с компоновкой автомобильного пульта.

### Новая карточка

- отдельная карточка устанавливается вместе с интеграцией и автоматически регистрируется в Home Assistant;
- центральная часть построена вокруг собственной SVG-иллюстрации Jolion-подобного SUV с крупной решёткой, фарами и шильдиком HAVAL;
- цвет кузова можно менять из визуального редактора;
- статусы замка, двигателя, дверей, окон, багажника, климата, T-Box и GPS могут отображаться вокруг автомобиля;
- информационные плитки поддерживают топливо, запас хода, пробег, давление шин, GSM/T-Box, GPS, климат, `oilQty`, время обновления и статусы подогревов передних сидений;
- круглые кнопки управления поддерживают замок, двигатель, климат, багажник, окна, refresh и доступные comfort-функции;
- optional-функции скрываются, если они отключены в capability-настройках интеграции;
- во время выполняющейся удалённой команды остальные remote-кнопки блокируются так же, как в основной карточке.

### Редактор / лицензирование

- добавлен собственный visual editor карточки Home Assistant с выбором кнопок, информационных плиток и статусов;
- можно изменить название карточки, цвет автомобиля и включить/отключить подтверждение remote-команд;
- компоновка вдохновлена идеей `Anonym-tsk/lovelace-starline-card`, но код, CSS и SVG-графика реализованы независимо;
- исходный код и PNG-assets GPL-3.0 проекта `lovelace-starline-card` в репозиторий не копировались;
- CI теперь проверяет синтаксис всех JS-файлов в `frontend/`.

### Протокол

- новых GWM remote payload в этой версии нет;
- существующая `custom:gwm-jolion-card` сохранена как первая карточка и продолжает работать независимо.

## 0.1.0-alpha.12

Ручная настройка опционального оборудования по комплектации автомобиля.

### Комплектация

- в настройках интеграции добавлены переключатели панорамы и шторки, обогрева руля, заднего и переднего defrost, электрообогрева лобового, подогрева передних сидений, Cabin Clean и очистителя воздуха;
- прежнее поведение сохраняется до первого изменения настроек: все ранее доступные функции считаются включёнными;
- `vehicleBasicsInfo` не используется как доказательство наличия оборудования, поскольку GWM возвращает универсальный шаблон даже для отсутствующих узлов;
- отключённые optional button/sensor/binary_sensor не загружаются как активные сущности;
- добавлен диагностический сенсор `Оборудование автомобиля` с текущими capability-флагами.

### Карточка / безопасность

- карточка скрывает панораму, шторку и comfort-функции, отключённые в настройках интеграции;
- backend блокирует remote-команды для оборудования, которое пользователь отметил как отсутствующее;
- добавлены unit-тесты ручных capability-настроек и сопоставления команд/состояний;
- новых GWM remote payload в этой версии нет.

## 0.1.0-alpha.11

Исправление разбора `vehicleBasicsInfo` для российского GWM Cloud без добавления новых команд автомобилю.

### Диагностика / API

- поддержана фактическая вложенность ответа `data → config`;
- безопасный snapshot теперь может включать полезные параметры климата, сидений, окон, defrost, `skyLight`, `shadeScreen` и таймеры comfort-функций;
- VIN, `userId`, подписка и неизвестные поля по-прежнему не попадают в whitelist snapshot;
- live-телеметрия подогрева сидений остаётся приоритетной, значения `vehicleBasicsInfo` используются только как fallback;
- `skyLight` и `shadeScreen` сохраняются как raw diagnostics без предположения о значении enum;
- добавлены unit-тесты для реальной структуры российского ответа и защиты от утечки неизвестных полей;
- remote payload и встроенная карточка в этой версии не изменялись.

## 0.1.0-alpha.10

Релиз надёжности без добавления новых неподтверждённых команд автомобиля.

### Надёжность

- удалённые T5-команды остаются строго последовательными: новая команда не принимается, пока предыдущая выполняется;
- cooldown переведён на монотонный таймер;
- перед удалённым запуском двигателя выполняется свежий опрос GWM и проверяются подтверждённые состояния: двигатель, центральный замок, двери и багажник;
- постоянная ошибка авторизации после автоматического повторного входа переводит интеграцию в Home Assistant reauth;
- повторная авторизация сохраняет существующий GWM device ID;
- ошибки результата T5 сохраняют код GWM, а timeout имеет отдельный диагностический код.

### Home Assistant / карточка

- сенсор последней команды использует состояния `выполняется`, `выполнено`, `ошибка` и публикует атрибут `status`;
- пока T5-команда выполняется, остальные удалённые кнопки карточки временно блокируются;
- карточка предварительно предупреждает о видимых препятствиях для запуска двигателя;
- новых remote payload в этой версии нет.

## 0.1.0-alpha.9

Исправление трактовки поля `oilQty` российского GWM API.

### Исправлено

- `oilQty` больше не трактуется как уровень топлива;
- диагностическая сущность переименована в `Уровень масла GWM (0–8)`;
- в карточке блок `Топливо GWM` заменён на `Масло`;
- уровень масла отображается как 8-сегментная шкала без вычисления процентов;
- обычный уровень топлива по-прежнему берётся из фактических литров и объёма бака;
- cache-busting встроенной карточки обновлён до `0.1.0-alpha.9`.

### Обоснование

В реальных ответах GWM `oilQty` не коррелирует с количеством топлива: при баке около 82–84% поле может иметь значение `4`. Российские реализации GWM также используют `oilQty` как уровень масла. Поэтому в этой версии поле отделено от топливной телеметрии.

## 0.1.0-alpha.8

Компактная карточка, подробный статус окон и графические индикаторы системы.

### Карточка

- заголовок теперь всегда одной строкой и одним шрифтом: модель + имя автомобиля из GWM API + `2WD / 4WD`, например `Haval Jolion Жулик 2WD`;
- удалена крупная декоративная иконка автомобиля;
- верхняя статистика стала компактной сеткой `Топливо л / Топливо % / Запас / Пробег`;
- кнопка `Окна` показывает конкретно открытые окна: переднее левое, переднее правое, заднее левое, заднее правое;
- подтверждена привязка телеметрии окон: `2210002` = переднее левое, `2210001` = переднее правое, `2210004` = заднее левое, `2210003` = заднее правое;
- при наличии открытого окна кнопка закрывает все окна;
- когда все окна закрыты, кнопка отправляет экспериментальную команду открытия всех окон;
- на кнопке `Обновить` показывается давность последнего успешного опроса GWM (`только что`, `N мин назад`, `N ч назад`);
- в разделе `Система` уровень GSM отображается графическими делениями вместо raw-числа;
- `oilQty` отображается как 8-сегментная шкала топлива GWM.

### Протокол

- добавлен экспериментальный сервис `open_windows` (`0x08`, `switchOrder=1`, четыре окна со значением `10`);
- команда закрытия окон остаётся подтверждённой и не изменена.

### Уточнение `oilQty`

- прежнее название `Уровень масла (raw)` было ошибочным;
- по открытым реализациям GWM `oilQty` — это не уровень моторного масла, а штатная шкала топлива `0–8` делений;
- поэтому сущность переименована в `Шкала топлива GWM (0–8)` и в карточке не выдаётся за уровень масла двигателя.

## 0.1.0-alpha.7

Версионирование HACS и доработка встроенной Lovelace-карточки.

### HACS / релизы

- добавлен GitHub Actions workflow, который создаёт GitHub Release и tag по версии из `manifest.json`;
- HACS теперь получает человекочитаемую версию (`0.1.0-alpha.7`, далее `alpha.8` и т. д.) вместо SHA коммита;
- релиз создаётся только для коммита, сообщение которого начинается с текущей версии, либо при ручном запуске workflow.

### Карточка

- убран принудительный заголовок `Haval Jolion`;
- в заголовке используется имя автомобиля из GWM, которое уже попадает в Home Assistant Device (`vehicleName`), рядом выводится `2WD` / `4WD`;
- модель `Haval Jolion` выводится второй строкой только если она не дублирует имя автомобиля;
- панорама и шторка перенесены непосредственно в раздел `Управление`;
- для панорамы теперь одна кнопка открыть/закрыть, для шторки — одна кнопка открыть/закрыть;
- так как подтверждённой телеметрии положения панорамы/шторки пока нет, при первом нажатии карточка просит выбрать `открыть` или `закрыть`, после успешной команды переключает действие локально до перезагрузки страницы;
- кнопка окон больше не выглядит отключённой, когда окна закрыты;
- если окна закрыты, нажатие объясняет, что подтверждена только команда закрытия; неподтверждённая команда открытия не отправляется;
- cache-busting URL карточки обновлён до `0.1.0-alpha.7`.

## 0.1.0-alpha.6

Обновление встроенной Lovelace-карточки и её отображения комплектации без изменения payload подтверждённых команд.

### Карточка

- рядом с `Haval Jolion` автоматически отображается `2WD` или `4WD`, когда код модификации известен;
- для российских модификаций Jolion используется подтверждённая карта: `CC7150BA00B` / `CC7150BA01B` → `2WD`, `CC7150BA24C` → `4WD`;
- добавлен блок управления панорамой: открыть / закрыть;
- добавлен блок управления шторкой панорамы: открыть / закрыть;
- команды панорамы и шторки помечены как экспериментальные, так как обратный статус положения пока не расшифрован;
- подогрев сидений и электрообогрев лобового остаются status-only: карточка явно показывает, что управляющий payload пока не подтверждён;
- сохранено управление закрытием окон;
- cache-busting URL карточки обновлён до `0.1.0-alpha.6`.

### Версионирование

- сообщения коммитов для релизных изменений начинаются с номера версии (`0.1.0-alpha.6`, далее `alpha.7` и т. д.); короткий SHA GitHub остаётся внутренним идентификатором коммита.

## 0.1.0-alpha.5

Обновление встроенной Lovelace-карточки без изменения протокола подтверждённых remote-команд.

### Карточка

- в верхнюю статистику добавлен `Топливо %`;
- подпись количества топлива изменена на `Топливо л`;
- убраны постоянные status chips `Закрыт`, `Двигатель ...`, `Климат ...`; предупреждения об открытых дверях, окнах и багажнике сохранены;
- удалён отдельный блок `Автомобиль`;
- в блок `Управление` добавлен таймер автозапуска двигателя 5–30 минут;
- выбранное время передаётся существующему сервису `gwm_jolion.start_engine` как `operation_time`;
- управление кондиционером перенесено в блок `Климат`;
- в блок `Климат` добавлены обогрев руля, обогрев заднего стекла и экспериментальный обдув лобового;
- статусы подогрева водительского/пассажирского сидений и электрообогрева лобового отображаются без управляющих команд, пока payload этих функций не подтверждён;
- обновлён cache-busting URL встроенной карточки.

### Безопасность

- для сидений и электрообогрева лобового карточка не отправляет неподтверждённые GWM-команды.

## 0.1.0-alpha.4

Безопасное обновление телеметрии и диагностики. Встроенная `gwm-jolion-card` и remote payloads не изменялись.

### Добавлено

- нормализованная модель `Haval Jolion` вместо внутреннего имени `HavalA01_...`;
- вычисляемый процент топлива по фактическим литрам и объёму бака;
- диагностические сенсоры двигателя, объёма бака, конфигурации, типа автомобиля, телематической платформы и raw-кода модели;
- диагностический сенсор состояния `vehicleBasicsInfo`;
- безопасная структура ответа `vehicleBasicsInfo` без значений, VIN, координат и других приватных данных;
- unit tests для нормализации модели, расчёта топлива и безопасного описания структуры ответа.

### Изменено

- ошибки GWM API теперь могут сохранять код ответа для диагностики;
- `vehicleBasicsInfo` различает `success_with_data`, `success_empty`, `unsupported` и `error`;
- информация автомобиля из `acquireVehicles` приводится к стабильным пользовательским полям.

## 0.1.0-alpha.3

Backend/diagnostics release. Встроенная `gwm-jolion-card` не изменялась.

### Добавлено

- безопасная диагностика Home Assistant с автоматическим удалением VIN, телефона, PIN, пароля, token, IMSI/ICCID и координат;
- история неизвестных GWM telemetry codes;
- счётчик неизвестных кодов как diagnostic sensor;
- время последнего успешного обновления GWM как timestamp sensor;
- безопасный whitelist snapshot `vehicleBasicsInfo`;
- informational capability framework для комплектаций Jolion;
- dependency-free protocol registry;
- четыре диагностических binary sensor для окон `2210001–2210004` без неподтверждённой привязки к физической позиции;
- `services.yaml` для всех зарегистрированных действий;
- английские переводы config/services;
- `PROTOCOL.md`;
- unit tests для protocol/capabilities;
- GitHub Actions: HACS validation, hassfest, compile/JSON/YAML/tests.

### Изменено

- удалённые команды дополнительно защищены lock от одновременного выполнения;
- последняя команда сразу получает состояние `pending` во время ожидания результата;
- `vehicleBasicsInfo` по-прежнему необязателен и не ломает основной polling при ошибке.

### Не изменялось

- frontend-файл `gwm-jolion-card.js`;
- payload существующих подтверждённых команд;
- climate payload;
- существующие unique IDs основных сущностей.
